DATABASE_URL=postgresql://elevia:change-me@localhost:5432/elevia
JWT_SECRET=change-me-too-please-use-a-long-random-string
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
RUST_LOG=info
# Bootstrapped as the first admin account on startup if no admin exists yet
# (there is no public endpoint to create one). Change the password after first login.
ADMIN_EMAIL=admin@elevia.local
ADMIN_PASSWORD=change-me-immediately
