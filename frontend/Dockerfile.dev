# Development image: Astro dev server with HMR. Source is bind-mounted by docker-compose.

FROM node:22-slim
WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

EXPOSE 4321
CMD ["npm", "run", "dev"]
